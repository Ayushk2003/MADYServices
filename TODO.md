- [x] Implement admin header Feedback tab UI with count badge.
- [x] Route `/feedback` to admin feedback view.
- [ ] Ensure `AdminRequests` renders dedicated feedback table on `/feedback`.
- [ ] Add responsive table styling for feedback rows (if missing).
- [ ] Re-run `npm run build` and `npm run dev` for final verification.

